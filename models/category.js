class Category {
  constructor(id, title,image,streams,isTop) {
    this.id = id;
    this.title = title;
    this.image = image;
    this.streams = streams;
    this.isTop = isTop;
    }
}

export default Category;
